<%= render_turbo_stream_flash_messages %>

<% if flash.none? %>
  <%= turbo_stream.update("channel_detail", "") %>
  <%= turbo_stream.remove("channel-item-#{@channel.id}") %>
  <%= turbo_stream.update("components-modal", "") %>
<% end %>
